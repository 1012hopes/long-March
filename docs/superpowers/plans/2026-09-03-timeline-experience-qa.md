# 时间线与叙事动效验收记录

日期：2026-09-03

## 自动化验证（已通过）

- `npm run test:timeline`：13/13 通过
- `npm run test:map`：2/2 通过
- `npm run test:markers`：4/4 通过
- `npm run test:learning`：4/4 通过
- `npm run test:stories`：8/8 通过
- `npm run build`：通过（TypeScript + Vite）
- `node scripts/validate-prep.mjs`：通过（`media-110` → `hist-001`，`media-111` → `hist-008`）
- `node app/scripts/contrast-audit.mjs`：全部对比度检查通过
- `npm audit --omit=dev --audit-level=high`：0 vulnerabilities
- `git diff --check`：通过

## 静态实现核对（已通过）

- 时间线节点使用 `timelineMarkers()` 和真实日期比例：通过（代码与测试核对）
- 左侧时间轴使用中性轨道与当前时间指针：通过（代码与样式核对）
- 左侧节点按钮命中区域达到 44×44px：通过（样式核对）
- 底部时间控件使用同一 `timelineT` / `onTimelineChange`：通过（代码与测试核对）
- 导览模式锁定底部拖动，避免停靠点与时间指针脱节：通过（代码与测试核对）
- 连续拖动不触发连续镜头飞行：通过（代码与测试核对）
- `prefers-reduced-motion` 下地图镜头、地形俯仰和滚动有即时降级：通过（代码与测试核对）
- 手机导览模式不显示无效的时间线抽屉入口：通过（代码与测试核对）

## 浏览器截图与交互验收（已通过）

使用 Microsoft Edge + Playwright 以精确视口完成截图复核：

- 1440×900：页面宽度与文档宽度均为 1440px，无横向溢出；时间线、地图和底栏层级清晰；
- 1920×1080：页面宽度与文档宽度均为 1920px，真实日期留白成立，底栏未遮挡主路线；
- 390×844 自由探索：页面宽度与文档宽度均为 390px，顶栏两行控件、底部摘要和时间入口无横向裁切；
- 390×844 节点学习：页面宽度与文档宽度均为 390px，节点正文正常换行，首屏未被隐藏时间线的 `scrollIntoView` 带偏；
- 390×844 时间抽屉：点击“时间与节点”后抽屉正常展开，节点热区和文本卡片可见，未产生横向溢出。

截图保存在本地验证目录 `.omx/screenshots/timeline/`（该目录不进入版本控制）。

视觉结论：通过。整体延续纸面档案与历史地图语言；真实时间轴、中性轨道、路线红焦点和底部时间控件形成统一层级，没有回到游戏式完成度表达。

## 阻塞项

无代码阻塞。

## 下一步

1. 在真实低性能手机上补充拖动帧率和 3D 地形加载体感测试；
2. 在目标投影设备上确认 1280×720 的文字可读性；
3. 后续视觉调整继续以截图复核和 90 分视觉门槛作为完成条件。
