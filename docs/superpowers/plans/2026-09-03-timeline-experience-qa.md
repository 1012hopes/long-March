# 时间线与叙事动效验收记录

日期：2026-09-03

## 自动化验证（已通过）

- `npm run test:timeline`：12/12 通过
- `npm run test:map`：2/2 通过
- `npm run test:markers`：4/4 通过
- `npm run test:learning`：4/4 通过
- `npm run test:stories`：8/8 通过
- `npm run build`：通过（TypeScript + Vite）
- `node scripts/validate-prep.mjs`：通过（`media-110` → `hist-001`，`media-111` → `hist-008`）
- `node app/scripts/contrast-audit.mjs`：全部对比度检查通过
- `npm audit --omit=dev --audit-level=high`：0 vulnerabilities
- `git diff --check`：通过

## 静态实现核对（已通过）

- 时间线节点使用 `timelineMarkers()` 和真实日期比例：通过（代码与测试核对）
- 左侧时间轴使用中性轨道与当前时间指针：通过（代码与样式核对）
- 左侧节点按钮命中区域达到 44×44px：通过（样式核对）
- 底部时间控件使用同一 `timelineT` / `onTimelineChange`：通过（代码与测试核对）
- 导览模式锁定底部拖动，避免停靠点与时间指针脱节：通过（代码与测试核对）
- 连续拖动不触发连续镜头飞行：通过（代码与测试核对）
- `prefers-reduced-motion` 下地图镜头、地形俯仰和滚动有即时降级：通过（代码与测试核对）
- 手机导览模式不显示无效的时间线抽屉入口：通过（代码与测试核对）

## 浏览器实机验收（待验证）

以下项目尚未在真实浏览器中完成截图、交互和性能核验；当前记录不能把它们标记为“通过”：

- 1440×900、1920×1080 下刻度文本的实际间距；
- 390×844 下抽屉、底部控件和时间标签的真实重叠情况；
- 不同字体渲染环境下的节点标题换行；
- 低性能移动设备上的拖动帧率和地图加载体感；
- 首次载入且 URL 恢复 `selectedNodeId` 时，左侧列表不会在首屏自动 `scrollIntoView`，但后续显式选点/重开面板仍会跟随滚动。

结论：自动化与静态检查已通过；真实浏览器下的视觉、交互和性能体验仍待人工复核。

## 阻塞项

无代码阻塞；缺少本环境内可复现的浏览器截图/交互验收链路。

## 下一步

1. 启动开发服务器，在桌面和手机尺寸完成一次真实截图与交互复核；
2. 重点确认首次载入恢复选中节点时的左侧列表滚动抑制，以及后续显式选点/重开面板的跟随滚动；
3. 如视觉细节或交互体感有偏差，再同步到样式和面板文档。
